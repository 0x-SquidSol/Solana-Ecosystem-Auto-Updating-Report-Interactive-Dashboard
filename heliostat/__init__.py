"""heliostat — the engine behind the Solana Ecosystem Report.

Collects on-chain and public off-chain data about the Solana network and
renders it as an HTML dashboard, a Markdown report, and structured JSON.
"""

__version__ = "0.1.0"
